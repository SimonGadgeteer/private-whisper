<Window x:Class="PrivateWhisper.DictionaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Private Whisper — Dictionary"
        Width="460" Height="480"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" Foreground="Gray" Margin="0,0,0,8"
                   Text="Names and jargon added here are biased into transcription and enforced during cleanup." />

        <DockPanel Grid.Row="1" Margin="0,0,0,8">
            <Button x:Name="AddButton" Content="Add" Width="70" DockPanel.Dock="Right" Margin="6,0,0,0" Click="AddButton_Click" />
            <TextBox x:Name="NewTermBox" KeyDown="NewTermBox_KeyDown" />
        </DockPanel>

        <ListBox x:Name="TermsList" Grid.Row="2" Margin="0,0,0,8" />

        <Button x:Name="RemoveButton" Grid.Row="3" Content="Remove selected" Width="130"
                HorizontalAlignment="Left" Margin="0,0,0,12" Click="RemoveButton_Click" />

        <StackPanel Grid.Row="4" Margin="0,0,0,8">
            <TextBlock Text="Suggestions from your corrections" FontWeight="Bold" Margin="0,0,0,4" />
            <ListBox x:Name="SuggestionsList" MinHeight="60" MaxHeight="110" />
            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                <Button x:Name="AcceptSuggestionButton" Content="Add to dictionary" Width="130" Click="AcceptSuggestionButton_Click" />
                <Button x:Name="DismissSuggestionButton" Content="Dismiss" Width="90" Margin="8,0,0,0" Click="DismissSuggestionButton_Click" />
            </StackPanel>
        </StackPanel>

        <Button Grid.Row="5" Content="Close" Width="90" HorizontalAlignment="Right" Click="CloseButton_Click" />
    </Grid>
</Window>
